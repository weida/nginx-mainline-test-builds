name: Build and Push Docker Images

on:
  push:
    branches: [ main ]
    paths:
      - 'Dockerfile'
      - 'Dockerfile.compat'
      - 'nginx-builder.sh'
      - '.github/workflows/docker-publish.yml'
  workflow_dispatch:
    inputs:
      force_build:
        description: 'Force rebuild even if version exists'
        required: false
        type: boolean
        default: false
  schedule:
    - cron: '0 2 * * *'

permissions:
  contents: write
  packages: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        variant:
          - { dockerfile: 'Dockerfile', suffix: '', glibc: '2.39', base: 'Ubuntu 24.04', cache_scope: 'nginx-standard', platforms: 'linux/amd64,linux/arm64' }
          - { dockerfile: 'Dockerfile.compat', suffix: '-compat', glibc: '2.17', base: 'CentOS 7 build + AlmaLinux 8 runtime', cache_scope: 'nginx-compat', platforms: 'linux/amd64,linux/arm64' }
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Get nginx version
      id: version
      run: |
        set -euo pipefail
        NGINX_VERSION="$(
          curl -fsSL --retry 3 --retry-delay 2 "https://api.github.com/repos/nginx/nginx/tags?per_page=100" |
            jq -r '.[].name | select(test("^release-[0-9]+\\.[0-9]+\\.[0-9]+$")) | sub("^release-"; "")' |
            sort -V |
            tail -1
        )"
        if [ -z "$NGINX_VERSION" ] || [ "$NGINX_VERSION" = "null" ]; then
          echo "Failed to resolve nginx version"
          exit 1
        fi
        echo "nginx_raw_tag=release-$NGINX_VERSION" >> $GITHUB_OUTPUT
        echo "nginx_version=$NGINX_VERSION" >> $GITHUB_OUTPUT
        echo "Nginx version: $NGINX_VERSION (glibc ${{ matrix.variant.glibc }})"
    
    - name: Check if version exists
      id: check
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.nginx_version }}"
        if [ -z "$VERSION" ]; then
          echo "nginx_version output is empty"
          exit 1
        fi
        if gh release view "v$VERSION" 2>/dev/null; then
          echo "exists=true" >> $GITHUB_OUTPUT
          echo "Version v$VERSION already exists"
        else
          echo "exists=false" >> $GITHUB_OUTPUT
          echo "Version v$VERSION is new"
        fi
    
    - name: Decide build
      id: decide
      run: |
        set -euo pipefail
        if [ "${{ github.event_name }}" = "schedule" ] && [ "${{ steps.check.outputs.exists }}" = "true" ] && [ "${{ inputs.force_build }}" != "true" ]; then
          echo "skip=true" >> $GITHUB_OUTPUT
          echo "Skipping scheduled build - version exists and force_build is false"
        else
          echo "skip=false" >> $GITHUB_OUTPUT
          echo "Proceeding with build"
        fi

    - name: Validate build metadata
      if: steps.decide.outputs.skip != 'true'
      run: |
        set -euo pipefail
        VERSION="${{ steps.version.outputs.nginx_version }}"
        USERNAME="${{ secrets.DOCKERHUB_USERNAME }}"
        IMAGE="${USERNAME}/nginx-http3:${VERSION}${{ matrix.variant.suffix }}"

        if [ -z "$USERNAME" ]; then
          echo "DOCKERHUB_USERNAME is empty"
          exit 1
        fi

        if [ -z "$VERSION" ]; then
          echo "nginx_version is empty"
          exit 1
        fi

        echo "Building image tags for $IMAGE"
    
    - name: Set up QEMU
      if: steps.decide.outputs.skip != 'true' && contains(matrix.variant.platforms, 'linux/arm64')
      uses: docker/setup-qemu-action@v3
    
    - name: Set up Docker Buildx
      if: steps.decide.outputs.skip != 'true'
      uses: docker/setup-buildx-action@v3
    
    - name: Login to Docker Hub
      if: steps.decide.outputs.skip != 'true'
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    - name: Build and push
      if: steps.decide.outputs.skip != 'true'
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ${{ matrix.variant.dockerfile }}
        platforms: ${{ matrix.variant.platforms }}
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest${{ matrix.variant.suffix }}
          ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:${{ steps.version.outputs.nginx_version }}${{ matrix.variant.suffix }}
        build-args: |
          NGINX_RAW_TAG=${{ steps.version.outputs.nginx_raw_tag }}
        cache-from: type=gha,scope=${{ matrix.variant.cache_scope }}
        cache-to: type=gha,mode=max,scope=${{ matrix.variant.cache_scope }}

    - name: Smoke test image
      if: steps.decide.outputs.skip != 'true'
      run: |
        set -euo pipefail
        IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:${{ steps.version.outputs.nginx_version }}${{ matrix.variant.suffix }}"
        NAME="nginx-smoke${{ matrix.variant.suffix }}"
        PORT=8080

        docker pull --platform linux/amd64 "$IMAGE"

        cleanup() {
          docker rm -f "$NAME" >/dev/null 2>&1 || true
        }
        trap cleanup EXIT

        docker run -d --rm --platform linux/amd64 --name "$NAME" -p ${PORT}:80 "$IMAGE"

        for _ in $(seq 1 20); do
          if curl -fsS "http://127.0.0.1:${PORT}/" >/tmp/nginx-smoke-response.txt; then
            test -s /tmp/nginx-smoke-response.txt
            exit 0
          fi
          sleep 1
        done

        docker ps -a
        docker logs "$NAME" >/tmp/nginx-smoke-logs.txt 2>&1 || true
        cat /tmp/nginx-smoke-logs.txt
        curl -fsS "http://127.0.0.1:${PORT}/" >/tmp/nginx-smoke-response.txt
    
    - name: Extract binaries from Docker image
      if: steps.decide.outputs.skip != 'true'
      run: |
        # Extract amd64 binary
        docker pull --platform linux/amd64 ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest${{ matrix.variant.suffix }}
        CONTAINER_AMD64=$(docker create --platform linux/amd64 ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest${{ matrix.variant.suffix }})
        docker cp $CONTAINER_AMD64:/usr/local/nginx ./nginx-amd64
        
        rm -f nginx-amd64/logs/access.log nginx-amd64/logs/error.log
        touch nginx-amd64/logs/access.log nginx-amd64/logs/error.log
        
        cp -r install-scripts/* nginx-amd64/
        chmod +x nginx-amd64/*.sh
        
        tar czf nginx-${{ steps.version.outputs.nginx_version }}-linux-amd64${{ matrix.variant.suffix }}.tar.gz -C nginx-amd64 .
        docker rm $CONTAINER_AMD64
        rm -rf nginx-amd64
        
        # Extract arm64 binary (only when platforms includes arm64)
        if [[ "${{ matrix.variant.platforms }}" == *"linux/arm64"* ]]; then
          docker pull --platform linux/arm64 ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest${{ matrix.variant.suffix }}
          CONTAINER_ARM64=$(docker create --platform linux/arm64 ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest${{ matrix.variant.suffix }})
          docker cp $CONTAINER_ARM64:/usr/local/nginx ./nginx-arm64
        
          rm -f nginx-arm64/logs/access.log nginx-arm64/logs/error.log
          touch nginx-arm64/logs/access.log nginx-arm64/logs/error.log
        
          cp -r install-scripts/* nginx-arm64/
          chmod +x nginx-arm64/*.sh
        
          tar czf nginx-${{ steps.version.outputs.nginx_version }}-linux-arm64${{ matrix.variant.suffix }}.tar.gz -C nginx-arm64 .
          docker rm $CONTAINER_ARM64
          rm -rf nginx-arm64
        fi
    
    - name: Upload binaries as artifact
      if: steps.decide.outputs.skip != 'true'
      uses: actions/upload-artifact@v4
      with:
        name: nginx-binaries${{ matrix.variant.suffix }}
        path: nginx-*.tar.gz
        retention-days: 7

  release:
    needs: docker
    if: |
      always() && !cancelled() &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || github.event_name == 'schedule') &&
      github.ref == 'refs/heads/main' &&
      needs.docker.result == 'success'
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Get nginx version
      id: version
      run: |
        set -euo pipefail
        NGINX_VERSION="$(
          curl -fsSL --retry 3 --retry-delay 2 "https://api.github.com/repos/nginx/nginx/tags?per_page=100" |
            jq -r '.[].name | select(test("^release-[0-9]+\\.[0-9]+\\.[0-9]+$")) | sub("^release-"; "")' |
            sort -V |
            tail -1
        )"
        if [ -z "$NGINX_VERSION" ] || [ "$NGINX_VERSION" = "null" ]; then
          echo "Failed to resolve nginx version"
          exit 1
        fi
        echo "nginx_version=$NGINX_VERSION" >> $GITHUB_OUTPUT
        echo "Nginx version: $NGINX_VERSION"

    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts

    - name: Prepare release files
      id: prepare
      run: |
        set -euo pipefail
        mkdir -p release-files
        find ./artifacts -name "*.tar.gz" -exec cp {} release-files/ \;
        ls -lh release-files/ || true
        if ! compgen -G "release-files/*.tar.gz" > /dev/null; then
          echo "No release tarballs found (likely scheduled skip with no new build); skipping release"
          echo "has_files=false" >> $GITHUB_OUTPUT
        else
          echo "has_files=true" >> $GITHUB_OUTPUT
        fi

    - name: Create GitHub Release
      if: steps.prepare.outputs.has_files == 'true'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.nginx_version }}
        name: Nginx ${{ steps.version.outputs.nginx_version }}
        body: |
          ## Nginx ${{ steps.version.outputs.nginx_version }} with HTTP/3 Support
          
          ### Features
          - ✅ HTTP/2 support
          - ✅ HTTP/3 (QUIC) support
          - ✅ TLS 1.3
          - ✅ Latest OpenSSL, PCRE2, and zlib
          - ✅ Multi-architecture support (amd64, arm64) for standard and compat
          - ✅ Multiple glibc versions for compatibility
          
          ### Docker Images
          
          **Standard (glibc 2.39+)** - Ubuntu 24.04 base:
          ```bash
          docker pull ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest
          docker pull ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:${{ steps.version.outputs.nginx_version }}
          ```
          
          **Compatible (glibc 2.17+)** - CentOS 7 build with AlmaLinux 8 minimal runtime:
          ```bash
          docker pull ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:latest-compat
          docker pull ${{ secrets.DOCKERHUB_USERNAME }}/nginx-http3:${{ steps.version.outputs.nginx_version }}-compat
          ```
          
          ### Binary Installation
          
          **Choose the right version:**
          - Standard: For Ubuntu 22.04+, Debian 12+, RHEL 9+ (glibc 2.35+)
          - Compat: For CentOS 7, Alibaba Cloud Linux 2/3, Ubuntu 20.04, Debian 11 (glibc 2.17+)
          
          **AMD64 (x86_64)**:
          ```bash
          # Standard version
          tar xzf nginx-${{ steps.version.outputs.nginx_version }}-linux-amd64.tar.gz
          
          # Compatible version
          tar xzf nginx-${{ steps.version.outputs.nginx_version }}-linux-amd64-compat.tar.gz
          
          # Install
          sudo ./install.sh
          sudo systemctl start nginx
          ```
          
          **ARM64 (aarch64)**:
          ```bash
          # Standard version
          tar xzf nginx-${{ steps.version.outputs.nginx_version }}-linux-arm64.tar.gz
          
          # Compatible version
          tar xzf nginx-${{ steps.version.outputs.nginx_version }}-linux-arm64-compat.tar.gz
          
          # Install
          sudo ./install.sh
          sudo systemctl start nginx
          ```
          
          ### Build Information
          - Nginx: ${{ steps.version.outputs.nginx_version }}
          - Architectures: amd64, arm64 (standard and compat; CentOS 7 SCL via archive.kernel.org centos-vault)
          - glibc versions: 2.39 (standard), 2.17 (compat)
          - Built: ${{ github.event.head_commit.timestamp }}
          - Commit: ${{ github.sha }}
        files: |
          release-files/*.tar.gz
        draft: false
        prerelease: false
        overwrite: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Update Docker Hub Description
      if: steps.prepare.outputs.has_files == 'true'
      run: |
        sudo apt-get update && sudo apt-get install -y jq
        chmod +x update-dockerhub-description.sh
        DOCKERHUB_TOKEN=${{ secrets.DOCKERHUB_TOKEN }} ./update-dockerhub-description.sh
